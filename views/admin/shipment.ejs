<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Pending Shipments</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
    <style>
      body {
        background: #f9fafb;
      }
      .card {
        border-radius: 15px;
        box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
        margin-bottom: 20px;
      }
      .card-header {
        background: linear-gradient(90deg, #6366f1, #3b82f6);
        color: white;
        border-radius: 15px 15px 0 0;
      }
      .status-badge {
        font-size: 0.9rem;
        padding: 5px 10px;
        border-radius: 8px;
      }
      .status-pending {
        background-color: #fbbf24;
        color: #1f2937;
      }
      .btn-ship {
        background: #10b981;
        color: white;
        border-radius: 8px;
        transition: 0.3s;
      }
      .btn-ship:hover {
        background: #059669;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h2 class="mb-4 text-center">📦 Pending Shipments</h2>

      <% if (orders.length === 0) { %>
      <div class="alert alert-info text-center">
        No pending shipments right now.
      </div>
      <% } else { %> <% orders.forEach(order => { %>
      <div class="card">
        <div class="card-header">Order ID: <%= order._id %></div>
        <div class="card-body">
          <h5 class="card-title">
            Product: <%= order.productId ? order.productId.name : "N/A" %>
          </h5>
          <p><strong>Quantity:</strong> <%= order.quantity %></p>
          <p><strong>Name:</strong> <%= order.name %></p>
          <p><strong>Mobile:</strong> <%= order.mobile %></p>
          <p>
            <strong>Address:</strong>
            <%= order.address.address %>, <%= order.address.locality %>, <%=
            order.address.city %>, <%= order.address.state %> - <%=
            order.address.pin %>
          </p>
          <% if (order.address.landmark) { %>
          <p><strong>Landmark:</strong> <%= order.address.landmark %></p>
          <% } %> <% if (order.address.alternateMobile) { %>
          <p>
            <strong>Alternate Mobile:</strong> <%= order.address.alternateMobile
            %>
          </p>
          <% } %>
          <p><strong>Payment Method:</strong> <%= order.paymentMethod %></p>
          <span class="status-badge status-pending">⏳ Pending</span>
          <div class="mt-3">
            <button class="btn btn-ship">Mark as Shipped</button>
          </div>
        </div>
      </div>
      <% }) %> <% } %>
    </div>
  </body>
</html>
