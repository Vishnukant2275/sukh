<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>FPO Login - Admin Portal</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(120deg, #6dd5ed, #2193b0);
        font-family: "Roboto", sans-serif;
        min-height: 100vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .login-container {
        background: #fff;
        padding: 2.5rem 2rem;
        border-radius: 16px;
        box-shadow: 0 8px 32px rgba(33, 147, 176, 0.2);
        width: 100%;
        max-width: 370px;
        text-align: center;
      }
      .login-container h2 {
        margin-bottom: 1.5rem;
        color: #2193b0;
        font-weight: 700;
        letter-spacing: 1px;
      }
      .login-container form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      .login-container input[type="text"],
      .login-container input[type="password"] {
        padding: 0.75rem 1rem;
        border: 1px solid #b2dfdb;
        border-radius: 8px;
        font-size: 1rem;
        outline: none;
        transition: border 0.2s;
      }
      .login-container input:focus {
        border-color: #2193b0;
      }
      .login-container button {
        background: #2193b0;
        color: #fff;
        border: none;
        padding: 0.75rem;
        border-radius: 8px;
        font-size: 1rem;
        font-weight: 700;
        cursor: pointer;
        transition: background 0.2s;
      }
      .login-container button:hover {
        background: #176582;
      }
      .divider {
        margin: 1.5rem 0;
        display: flex;
        align-items: center;
        text-align: center;
      }
      .divider::before,
      .divider::after {
        content: "";
        flex: 1;
        border-bottom: 1px solid #b2dfdb;
      }
      .divider:not(:empty)::before {
        margin-right: 0.75em;
      }
      .divider:not(:empty)::after {
        margin-left: 0.75em;
      }
      .google-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        background: #fff;
        color: #444;
        border: 1px solid #b2dfdb;
        font-weight: 500;
        padding: 0.7rem;
        border-radius: 8px;
        cursor: pointer;
        transition: background 0.2s, border 0.2s;
      }
      .google-btn:hover {
        background: #f1f1f1;
        border-color: #2193b0;
      }
      .google-icon {
        width: 22px;
        height: 22px;
      }
      .signup-link {
        display: block;
        margin-top: 1.5rem;
        font-size: 1rem;
        color: #2193b0;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s;
      }
      .signup-link:hover {
        color: #176582;
        text-decoration: underline;
      }
      .error-message {
        background: #ffeaea;
        color: #d8000c;
        border: 1px solid #d8000c;
        border-radius: 8px;
        padding: 0.75em 1.2em;
        margin-bottom: 1.2em;
        font-size: 1.05em;
        font-weight: 500;
        box-shadow: 0 2px 8px rgba(216, 0, 12, 0.07);
        text-align: center;
      }
      @media (max-width: 480px) {
        .login-container {
          padding: 1.5rem 0.5rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="login-container">
      <h2>Welcome Admin! Login here to continue</h2>
      <form action="/admin/login" method="POST">
        <input
          type="text"
          name="email"
          placeholder="Enter your Email"
          required
        />
        <div
          style="
            display: flex;
            align-items: center;
            position: relative;
            margin-bottom: 1em;
            max-width: 350px;
          "
        >
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Password"
            required
            style="
              flex: 1;
              padding: 0.7em 2.5em 0.7em 1em;
              font-size: 1em;
              border-radius: 6px;
              border: 1px solid #ccc;
            "
          />
          <button
            type="button"
            id="togglePassword"
            style="
              position: absolute;
              right: 0.8em;
              background: none;
              border: none;
              cursor: pointer;
              padding: 0;
              height: 100%;
              display: flex;
              align-items: center;
            "
            tabindex="-1"
          >
            <span id="eyeIcon" style="font-size: 1.2em">&#128065;</span>
          </button>
        </div>
        <button type="submit">Login</button>
      </form>
      <div class="divider">or</div>

      <a href="/admin/signup" class="signup-link"
        >Not registered? Sign up here</a
      >
      <a href="admin/forgot-password" class="signup-link">Forgot Password?</a>
    </div>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="error-message"><%= error %></div>
    <% } %>
    <script>
      const passwordInput = document.getElementById("password");
      const togglePassword = document.getElementById("togglePassword");
      const eyeIcon = document.getElementById("eyeIcon");

      togglePassword.addEventListener("click", function () {
        const type = passwordInput.type === "password" ? "text" : "password";
        passwordInput.type = type;
        // Change eye icon based on password visibility
        eyeIcon.textContent = type === "password" ? "🫣" : "😏";
      });
    </script>
  </body>
</html>
