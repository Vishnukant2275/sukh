<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Payment | <%= order.name %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(135deg, #cce5ff, #e6ffe6);
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }
      .card {
        border-radius: 1.2rem;
        box-shadow: 0 8px 18px rgba(0, 0, 0, 0.1);
      }
      .upi-box {
        background: #fff5e6;
        border: 2px dashed #ff9800;
        padding: 20px;
        border-radius: 12px;
        text-align: center;
        margin-top: 15px;
      }
      .upi-id {
        font-weight: bold;
        color: #d35400;
        font-size: 1.2rem;
      }
      .btn-copy {
        margin-top: 10px;
      }
      .success-text {
        font-size: 1.1rem;
        color: #28a745;
        font-weight: 500;
        margin-top: 15px;
      }
    </style>
  </head>
  <body>
    <div class="container py-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card p-4">
            <h3 class="text-center text-primary mb-3">Complete Your Payment</h3>
            <p class="text-muted text-center">
              Hi <strong><%= order.name %></strong>, please pay to confirm your
              order.
            </p>

            <!-- Order Summary -->
            <div class="mb-4">
              <h5>Order Summary</h5>
              <ul class="list-group">
                <li class="list-group-item d-flex justify-content-between">
                  <span>Product</span>
                  <strong><%= product.name %></strong>
                </li>
                <li class="list-group-item d-flex justify-content-between">
                  <span>Quantity</span>
                  <strong><%= order.quantity %></strong>
                </li>
                <li class="list-group-item d-flex justify-content-between">
                  <span>Total</span>
                  <strong class="text-success"
                    >₹ <%= product.price * order.quantity %></strong
                  >
                </li>
              </ul>
            </div>
             <a href="<%= upiLink %>" style="text-decoration: none;">
            <!-- UPI Payment -->
            <div class="upi-box">
              <p>Pay using UPI</p>
              
              <!-- QR Code -->
              <img
                src="<%= qrCode %>"
                alt="UPI QR Code"
                class="img-fluid mb-3"
                style="max-width: 200px"
              />

              <!-- UPI ID -->
              <div class="upi-id"><P>Click the Qr to Pay</P></div>

             

              <!-- Click to Pay -->
              <div class="mt-3">
                <a href="<%= upiLink %>" class="btn btn-success w-100">
                  💳 Click Here to Pay
                </a>
               
              </div> </a>
              <div class="text-center mt-4">
                <button class="btn btn-success px-4 py-2" onclick="paid()">I Have Paid</button>
                <p class="text-muted mt-2">
                  After paying, click this button to confirm.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script>
      function paid(){
        res.redirect("/profile")
      }

      
    </script>
  </body>
</html>
