<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Sell Raw Material - FPO</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f4f8f6;
        margin: 0;
        padding: 0;
      }
      .navbar {
        background: #2d6a4f;
        padding: 0 20px;
        display: flex;
        align-items: center;
        height: 56px;
        position: relative;
        z-index: 2;
      }
      .navbar ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: row;
        transition: max-height 0.3s;
      }
      .navbar li {
        margin-right: 30px;
      }
      .navbar a {
        color: #fff;
        text-decoration: none;
        font-size: 17px;
        font-weight: 500;
        transition: color 0.2s;
      }
      .navbar a:hover {
        color: #b7e4c7;
      }
      .navbar .logout-btn {
        margin-left: auto;
        color: #fff;
        text-decoration: none;
        font-size: 17px;
        font-weight: 500;
        position: relative;
        z-index: 3;
      }
      .navbar .menu-toggle {
        display: none;
        background: none;
        border: none;
        color: #fff;
        font-size: 28px;
        margin-right: 10px;
        cursor: pointer;
      }
      .container {
        max-width: 500px;
        margin: 40px auto;
        background: #fff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        position: relative;
        z-index: 1;
      }
      h2 {
        text-align: center;
        color: #2d6a4f;
      }
      label {
        display: block;
        margin-top: 15px;
        font-weight: bold;
      }
      input,
      select,
      textarea {
        width: 100%;
        padding: 8px;
        margin-top: 5px;
        border-radius: 4px;
        border: 1px solid #ccc;
      }
      button[type="submit"] {
        margin-top: 20px;
        width: 100%;
        padding: 10px;
        background: #2d6a4f;
        color: #fff;
        border: none;
        border-radius: 4px;
        font-size: 16px;
        cursor: pointer;
      }
      button[type="submit"]:hover {
        background: #40916c;
      }

      @media (max-width: 768px) {
        .navbar {
          flex-direction: column;
          align-items: stretch;
          height: auto;
          padding: 0;
          position: relative;
        }
        .navbar .menu-toggle {
          display: block;
          margin: 10px 0 0 15px;
          align-self: flex-start;
        }
        .navbar ul {
          flex-direction: column;
          width: 100%;
          background: #2d6a4f;
          max-height: 500px;
          overflow: hidden;
          transition: max-height 0.3s;
        }
        .navbar ul.closed {
          max-height: 0;
          padding: 0;
          border: none;
        }
        .navbar ul.open {
          max-height: 500px;
        }
        .navbar li {
          margin: 0;
          padding: 12px 20px;
          border-bottom: 1px solid #40916c;
        }
        .navbar .logout-btn {
          width: auto;
          margin: 0;
          padding: 10px 20px;
          border-top: none;
          border-bottom: none;
          position: absolute;
          top: 10px;
          right: 15px;
        }
        .container {
          margin-top: 20px;
        }
      }
    </style>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <nav class="navbar">
      <button
        class="menu-toggle"
        aria-label="Toggle navigation"
        style="margin-left: 0; margin-right: auto"
      >
        &#9776;
      </button>
      <ul class="open">
        <li><a href="/home">Home</a></li>
        <li><a href="/buy">Buy</a></li>
        <li><a href="/sell">Sell</a></li>
        <li><a href="/aboutus">About Us</a></li>
        <li><a href="/contact">Contact Us</a></li>
        <li><a href="/profile">Profile</a></li>
      </ul>
      <a href="/logout" class="logout-btn btn btn-outline-success">Logout</a>
    </nav>
    <div class="container">
      <h2><%= user.name%>, Sell Your Raw Material to Us</h2>
      <p style="color: red; font-weight: bold; margin-bottom: 10px">
        <span style="text-decoration: underline">Note:</span> 1. Please enter
        only that number which can be contacted.<br />
        2. Price will be decided after analysing the quality of raw material.<br />
        3. Please share your full address with landmark.
      </p>
      <form action="/sell" method="POST">
        <label for="contact">Contact Number in use</label>
        <input
          type="tel"
          id="contact"
          name="contact"
          required
          pattern="[0-9]{10}"
          maxlength="10"
        />

        <label for="material">Raw Material</label>
        <input
          type="text"
          id="material"
          name="material"
          placeholder="e.g., Wheat, Rice, Maize"
          required
        />

        <label for="quantity">Quantity (in kg)</label>
        <input type="number" id="quantity" name="quantity" min="1" required />

        <label for="location">Location/Address</label>
        <input type="text" id="location" name="location" required />

        <label for="notes">Additional Notes</label>
        <textarea
          id="notes"
          name="notes"
          rows="3"
          placeholder="Any special information..."
        ></textarea>

        <button type="submit">Submit</button>
      </form>
    </div>
    <script>
      const menuToggle = document.querySelector(".menu-toggle");
      const navUl = document.querySelector(".navbar ul");

      function handleResize() {
        if (window.innerWidth <= 768) {
          navUl.classList.add("open");
          navUl.classList.remove("closed");
        } else {
          navUl.classList.remove("open");
          navUl.classList.remove("closed");
        }
      }

      menuToggle.addEventListener("click", function () {
        if (window.innerWidth <= 768) {
          navUl.classList.toggle("open");
          navUl.classList.toggle("closed");
        }
      });

      window.addEventListener("resize", handleResize);
      window.addEventListener("DOMContentLoaded", handleResize);
    </script>
  </body>
</html>
