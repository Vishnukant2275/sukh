<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>FPO Home - Welcome</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Google Fonts for modern look -->
    <link
      href="https://fonts.googleapis.com/css?family=Montserrat:700,400&display=swap"
      rel="stylesheet"
    />
    <!-- SwiperJS Carousel CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"
    />
    <style>
      body {
        font-family: "Montserrat", Arial, sans-serif;
        background: linear-gradient(135deg, #e0ffe6 0%, #b3e0ff 100%);
        margin: 0;
        padding: 0;
        min-height: 100vh;
      }
      header {
        background: rgba(46, 139, 87, 0.95);
        color: #fff;
        padding: 2rem 0 1rem 0;
        text-align: center;
        letter-spacing: 2px;
        box-shadow: 0 2px 12px rgba(46, 139, 87, 0.08);
      }
      header h1 {
        font-size: 2.5rem;
        margin: 0 0 0.5rem 0;
        font-weight: 700;
        letter-spacing: 1px;
      }
      header p {
        font-size: 1.2rem;
        margin: 0;
        font-weight: 400;
        letter-spacing: 1px;
      }
      nav {
        display: flex;
        justify-content: center;
        gap: 1.5rem;
        margin: 1.5rem 0 0.5rem 0;
        flex-wrap: wrap;
      }
      .nav-btn {
        background: #fff;
        color: #2e8b57;
        border: 2px solid #2e8b57;
        border-radius: 30px;
        padding: 0.8rem 2rem;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s;
        box-shadow: 0 2px 8px rgba(46, 139, 87, 0.08);
        outline: none;
      }
      .nav-btn:hover,
      .nav-btn.active {
        background: #2e8b57;
        color: #fff;
        transform: translateY(-2px) scale(1.04);
        border-color: #1e5e3d;
      }
      .hero-section {
        max-width: 1100px;
        margin: 2rem auto 0 auto;
        border-radius: 18px;
        overflow: hidden;
        box-shadow: 0 4px 24px rgba(46, 139, 87, 0.13);
        background: #fff;
        position: relative;
      }
      .swiper {
        width: 100%;
        height: 340px;
      }
      .swiper-slide {
        display: flex;
        align-items: center;
        justify-content: center;
        background: #f3fff7;
        position: relative;
      }
      .swiper-slide img {
        width: 100%;
        height: 340px;
        object-fit: cover;
        object-position: center;
        filter: brightness(0.93) contrast(1.05);
      }
      .slide-caption {
        position: absolute;
        bottom: 24px;
        left: 32px;
        background: rgba(46, 139, 87, 0.85);
        color: #fff;
        padding: 0.7rem 1.5rem;
        border-radius: 12px;
        font-size: 1.15rem;
        font-weight: 500;
        box-shadow: 0 2px 8px rgba(46, 139, 87, 0.15);
        max-width: 80%;
      }
      .container {
        max-width: 900px;
        margin: 2.5rem auto;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.97);
        border-radius: 18px;
        box-shadow: 0 4px 24px rgba(46, 139, 87, 0.1);
      }
      .about-section {
        margin-top: 1rem;
        text-align: center;
      }
      .about-section h2 {
        color: #2e8b57;
        margin-bottom: 1rem;
        font-size: 2rem;
        font-weight: 700;
      }
      .about-section p {
        color: #444;
        font-size: 1.13rem;
        line-height: 1.7;
        margin: 0 auto;
        max-width: 700px;
      }
      /* Community Section */
      .community-section {
        max-width: 1100px;
        margin: 2rem auto 0 auto;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.97);
        border-radius: 18px;
        box-shadow: 0 4px 24px rgba(46, 139, 87, 0.1);
      }
      .community-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        flex-wrap: wrap;
        gap: 1rem;
      }
      .community-header h2 {
        color: #2e8b57;
        font-size: 2rem;
        font-weight: 700;
        margin: 0;
      }
      .add-post-btn {
        background: #2e8b57;
        color: #fff;
        border: none;
        border-radius: 30px;
        padding: 0.7rem 2rem;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s;
        box-shadow: 0 2px 8px rgba(46, 139, 87, 0.08);
      }
      .add-post-btn:hover {
        background: #1e5e3d;
      }
      .community-cards {
        display: flex;
        flex-wrap: wrap;
        gap: 2rem;
        justify-content: center;
      }
      .community-card {
        background: #f3fff7;
        border-radius: 14px;
        box-shadow: 0 2px 12px rgba(46, 139, 87, 0.08);
        padding: 1.5rem;
        width: 310px;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        position: relative;
        min-height: 220px;
      }
      .community-card .farmer-avatar {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        object-fit: cover;
        margin-bottom: 0.7rem;
        border: 2px solid #2e8b57;
      }
      .community-card .farmer-name {
        font-weight: 700;
        color: #2e8b57;
        font-size: 1.1rem;
        margin-bottom: 0.3rem;
      }
      .community-card .post-date {
        font-size: 0.93rem;
        color: #888;
        margin-bottom: 0.7rem;
      }
      .community-card .post-content {
        color: #333;
        font-size: 1.08rem;
        margin-bottom: 0.7rem;
        flex: 1;
      }
      .community-card .post-image {
        width: 100%;
        border-radius: 10px;
        margin-bottom: 0.7rem;
        object-fit: cover;
        max-height: 120px;
      }
      @media (max-width: 900px) {
        .hero-section,
        .container,
        .community-section {
          margin-left: 1rem;
          margin-right: 1rem;
        }
        .community-cards {
          gap: 1rem;
        }
      }
      @media (max-width: 600px) {
        nav {
          flex-direction: column;
          gap: 0.7rem;
        }
        .container,
        .community-section {
          padding: 1rem;
        }
        .hero-section {
          margin-top: 1rem;
        }
        .swiper,
        .swiper-slide img {
          height: 180px;
        }
        .slide-caption {
          font-size: 0.98rem;
          left: 10px;
          bottom: 10px;
          padding: 0.5rem 1rem;
        }
        .community-cards {
          flex-direction: column;
          gap: 1rem;
        }
        .community-card {
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <h1>
        Welcome <%= isLoggedIn?user.name:'Guest'%> to Sugam Uday Krishi Haat
      </h1>
      <p>Empowering Farmers. Connecting Communities.</p>
      <button
        class="logout-btn"
        onclick="location.href=`<%= isLoggedIn?'/Logout':'/Login'%>`"
      >
        <%= isLoggedIn?'Logout':'Login'%>
      </button>
    </header>
    <style>
      .logout-btn {
        position: absolute;
        top: 24px;
        right: 32px;
        background: #fff;
        color: #2e8b57;
        border: 2px solid #2e8b57;
        border-radius: 30px;
        padding: 0.6rem 1.5rem;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s;
        box-shadow: 0 2px 8px rgba(46, 139, 87, 0.08);
        outline: none;
        z-index: 10;
      }
      .logout-btn:hover {
        background: #2e8b57;
        color: #fff;
        border-color: #1e5e3d;
      }
      @media (max-width: 600px) {
        .logout-btn {
          top: 12px;
          right: 12px;
          padding: 0.5rem 1rem;
          font-size: 0.95rem;
        }
      }
      header {
        position: relative;
      }
    </style>
    <nav>
      <button class="nav-btn" onclick="location.href='/buy'">Buy</button>
      <button class="nav-btn" onclick="location.href='/sell'">Sell</button>

      <button class="nav-btn" onclick="location.href='/aboutus'">
        About Us
      </button>
      <button class="nav-btn" onclick="location.href='/contact'">
        Contact Us
      </button>
      <% if (isLoggedIn) { %>
      <button class="nav-btn" onclick="location.href='/profile'">
        My Profile
      </button>
      <% } %>
    </nav>
    

    <!-- Community Section -->
    <div class="community-section">
      <div class="community-header">
        <h2>Community Posts</h2>
        <button class="add-post-btn" onclick="location.href='/add-post'">
          + Add Post
        </button>
      </div>
      <div class="community-cards">
        <% posts.forEach(post => { %>
        <div class="community-card">
          <!-- Farmer avatar -->
          <img
            class="farmer-avatar"
            src="<%= post.userId && post.userId.avatar ? post.userId.avatar : 'https://randomuser.me/api/portraits/lego/1.jpg' %>"
            alt="Farmer <%= post.userId ? post.userId.name : 'Unknown' %>"
          />

          <!-- Farmer name -->
          <div class="farmer-name">
            <%= post.userId ? post.userId.name : 'Unknown' %>
          </div>

          <!-- Post date -->
          <div class="post-date"><%= post.createdAt.toDateString() %></div>

          <!-- Post content -->
          <div class="post-content"><%= post.description %></div>

          <!-- Post image -->
          <% if (post.image) { %>
          <img
            class="post-image"
            src="data:<%= post.image.contentType %>;base64,<%= post.image.data.toString('base64') %>"
            alt="Post Image"
          />
          <% } else { %>
          <span>No Image</span>
          <% } %>
        </div>
        <% }) %>
      </div>
    </div>

    <div class="container">
      <div class="about-section">
        <h2>About Our FPO Platform</h2>
        <p>
          Our Farmer Producer Organization (FPO) platform is dedicated to
          supporting farmers by providing a seamless marketplace to buy and sell
          produce, connect with the farming community, access real-time market
          prices and weather updates, and get support. Join our vibrant
          community, post your updates, and grow together!
        </p>
      </div>
    </div>
    <!-- SwiperJS Carousel JS -->
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script>
      new Swiper(".swiper", {
        loop: true,
        autoplay: {
          delay: 3500,
          disableOnInteraction: false,
        },
        pagination: {
          el: ".swiper-pagination",
          clickable: true,
        },
        navigation: {
          nextEl: ".swiper-button-next",
          prevEl: ".swiper-button-prev",
        },
        effect: "fade",
        fadeEffect: {
          crossFade: true,
        },
      });
    </script>
  </body>
</html>
