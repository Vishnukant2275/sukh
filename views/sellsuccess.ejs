<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Sell Request Submitted</title>
    <link
      href="https://fonts.googleapis.com/css?family=Montserrat:600,400&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Montserrat", Arial, sans-serif;
        background: linear-gradient(135deg, #e0ffe9 0%, #c3eaff 100%);
        min-height: 100vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .success-container {
        background: #fff;
        padding: 48px 36px 40px 36px;
        border-radius: 18px;
        box-shadow: 0 8px 32px rgba(44, 62, 80, 0.12);
        text-align: center;
        max-width: 400px;
        width: 100%;
        animation: fadeIn 1s;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      .success-icon {
        background: linear-gradient(135deg, #4fd1c5 0%, #38a169 100%);
        color: #fff;
        border-radius: 50%;
        width: 70px;
        height: 70px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 40px;
        margin: 0 auto 24px auto;
        box-shadow: 0 4px 16px rgba(72, 187, 120, 0.15);
      }
      h1 {
        margin-bottom: 12px;
        color: #2d3748;
        font-size: 2rem;
        font-weight: 600;
        letter-spacing: 0.5px;
      }
      p {
        color: #4a5568;
        font-size: 1.08rem;
        margin-bottom: 28px;
        line-height: 1.6;
      }
      .back-btn {
        display: inline-block;
        padding: 12px 32px;
        background: linear-gradient(90deg, #38a169 0%, #4fd1c5 100%);
        color: #fff;
        border: none;
        border-radius: 24px;
        font-size: 1rem;
        font-weight: 600;
        text-decoration: none;
        transition: background 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 8px rgba(72, 187, 120, 0.1);
        cursor: pointer;
      }
      .back-btn:hover {
        background: linear-gradient(90deg, #319795 0%, #2f855a 100%);
        box-shadow: 0 4px 16px rgba(72, 187, 120, 0.18);
      }
    </style>
  </head>
  <body>
    <div class="success-container">
      <div class="success-icon">
        <svg width="38" height="38" viewBox="0 0 38 38" fill="none">
          <circle cx="19" cy="19" r="19" fill="none" />
          <path
            d="M12 20.5L17 25.5L27 15.5"
            stroke="white"
            stroke-width="3"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
      </div>
      <h1>Submission Successful!</h1>
      <p>
        We’ve received your selling request.<br />
        Our team will review your details and get in touch with you soon.<br />
        Thank you for choosing us!
      </p>
      <a href="/home" class="back-btn">Back to Home</a>
    </div>
  </body>
</html>
